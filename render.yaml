services:
  - type: web
    name: report-app
    runtime: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && MPLCONFIGDIR=/tmp/matplotlib uvicorn app.main:app --host 0.0.0.0 --port $PORT
